from django.urls import path
from . import views

urlpatterns = [
    # path('post_mytimeline/', views.MyTimeLinesView.as_view(), name="post_mytimeline"),
    # path('mytimelines/<user_id>/', views.MyTimeLinesView.as_view(), name="mytimelines"),
    # path('like_unlike/', views.PostLikeView.as_view(), name="like_unlike"),

    # path('timelines/<user_id>/', views.TimelinesView.as_view(), name="timelines"),
    # path('other_timelines/<other_id>/', views.OtherTimelinesView.as_view(), name="othertimelines"),

    # path('comments/', views.CommentsView.as_view(), name="comments"),
    # path('comments/<post_id>/<user_id>/', views.CommentsView.as_view(), name="comments"),

    # path('subcomments/', views.SubCommentsView.as_view(), name="subcomments"),
    # path('test_url/', views.testing.as_view(), name="test_url"),
]